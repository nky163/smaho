version: "3"

services:
  front:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.front
    ports:
    - "3000:3000"
    volumes:
      - ./front:/app/front
    entrypoint: "npm run serve"
    tty: true


